# Pin the Rust toolchain so local and CI builds are reproducible. rustup reads
# this file and installs the specified version and components automatically on
# the first cargo invocation.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
